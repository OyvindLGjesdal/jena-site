name: Check Markdown links
# manual
on: workflow_dispatch

jobs:
  linkChecker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Link Checker
        uses: oyvindlgjesdal/lychee-action@v1.0.8
        with:
          args: --verbose --no-progress source/**/*.md
