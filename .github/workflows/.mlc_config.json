{ "replacementPatterns": [
        {
            "pattern": "^/",
            "replacement": "https://jena.apache.org/"
        },
        {
            "pattern": "^([^:]+)$",
            "replacement": "https://jena.apache.org/{{BASEURL}}/$1"
        }

    ]
}
