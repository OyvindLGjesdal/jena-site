{
                ignorePatterns: [{ pattern: "^([^:]+)$" }],

 "replacementPatterns": [
        {
            "pattern": "^/",
            "replacement": "https://jena.apache.org/"
        }

    ]
}
